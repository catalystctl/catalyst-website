---
export const prerender = false;

const slug = Astro.params.slug ?? "";
const path = slug ? `${slug}/` : "";
const target = `https://docs.catalystctl.com/${path}${Astro.url.search}`;
return Astro.redirect(target, 301);
---
